<h1>掲示板一覧</h1>
<div class="posts">
  <% @posts.each do |post| %>
    <div class="post">
      <img src="<%= post.thumbnail %>" alt="<%= post.title %>のサムネイル">
      <h2><%= post.title %></h2>
      <p>投稿者: <%= post.user_name %></p>
    </div>
  <% end %>
</div>
